# Select Region
To cut a N dimensional part of an array, we use the following methods:

```@docs
select_region
select_region!
select_region_view
```

## Slice
```@docs
slice
NDTools.slice_indices
```
